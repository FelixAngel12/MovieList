<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movielist">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieList">
        <activity
            android:name=".MainActivity2"
            android:exported="false" />
        <activity
            android:name=".DescripcionPeliculas"
            android:exported="false" />
        <activity
            android:name=".Descrip"
            android:exported="false"
            android:label="@string/title_activity_descrip"
            android:theme="@style/Theme.MovieList.NoActionBar" />
        <activity
            android:name=".Descripcion"
            android:exported="false"
            android:label="@string/title_activity_descripcion"
            android:theme="@style/Theme.MovieList.NoActionBar" />
        <activity
            android:name=".DescripcionView"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>